{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit *)",
            "command": "$HOME/bin/forge-dispatch-review",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
